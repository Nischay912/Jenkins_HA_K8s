apiVersion: v1
kind: Service
metadata:
  name: jenkins-ui
  namespace: {{ .Values.namespace }}
spec:
  type: NodePort
  selector:
    app: jenkins-ha
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: {{ .Values.service.nodePort }}
